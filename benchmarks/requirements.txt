# Core dependencies for benchmarks
numpy>=1.20.0
torch>=2.0.0
Pillow>=9.0.0

# Format-specific dependencies
h5py>=3.0.0
webdataset>=0.2.0
pyarrow>=10.0.0  # For Parquet comparison

# Utilities
tqdm>=4.60.0
psutil>=5.8.0
tabulate>=0.8.0

# Data generation
scikit-image>=0.19.0  # For realistic image-like patterns
