# Requirements for running the ML efficiency examples
numpy>=1.20.0
pillow>=9.0.0
tqdm>=4.60.0

# Optional: for PyTorch integration demo
torch>=2.0.0

# Optional: for HDF5 comparison benchmark
h5py>=3.0.0

# Note: Install Strata with:
# maturin develop --manifest-path ../../crates/loader/Cargo.toml
