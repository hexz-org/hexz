[package]
name = "hexz-store"
version.workspace = true
edition.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
description = "Storage backends for Hexz snapshots (local file, HTTP, S3)"

[dependencies]
hexz-common.workspace = true
hexz-core = { workspace = true, default-features = false, features = ["compression-zstd", "encryption"] }
bytes.workspace = true
memmap2 = "0.9"
tokio = { workspace = true, features = ["rt", "time"] }
reqwest = { version = "0.11", default-features = false, features = ["rustls-tls", "stream"] }
url = "2.5"

[dependencies.rust-s3]
version = "0.33"
default-features = false
features = ["tokio-rustls-tls", "fail-on-err"]
optional = true

[dev-dependencies]
tempfile = "3.8"
rand = { workspace = true }
wiremock = "0.6"
rstest = "0.18"

[features]
default = ["s3"]
s3 = ["dep:rust-s3"]
